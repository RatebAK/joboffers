meta {
  name: Withdraw Application
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/api/job-seeker/applications/{{application_id}}/withdraw
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
