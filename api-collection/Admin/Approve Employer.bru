meta {
  name: Approve Employer
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/admin/employers/{{employer_application_id}}/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
