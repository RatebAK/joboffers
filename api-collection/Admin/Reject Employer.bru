meta {
  name: Reject Employer
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/admin/{{employer_application_id}}/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "review_notes": "Insufficient documentation provided."
  }
}
