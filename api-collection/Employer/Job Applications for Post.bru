meta {
  name: Job Applications for Post
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/employer/jobs/{{job_id}}/applications
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  ~per_page: 15
  ~page: 1
}
