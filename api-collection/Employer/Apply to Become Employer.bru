meta {
  name: Apply to Become Employer
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/employer/apply
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  document: @file(/path/to/your-document.pdf)
}
