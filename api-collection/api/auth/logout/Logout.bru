meta {
  name: Logout
  type: http-request
  seq: 1
}

http {
  method: POST
  url: {{baseUrl}}/api/auth/logout
  body: none
  auth: inherit
}

example {
  name: 200 Response
  
  request: {
    url: {{baseUrl}}/api/auth/logout
    method: POST
    mode: none
  }
  
  response: {
    headers: {
      Content-Type: application/json
    }
  
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: json
      content: '''
        {
          "message": "User successfully signed out"
        }
      '''
    }
  }
}
